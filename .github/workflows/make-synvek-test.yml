name: Make synvek test

on:
    workflow_dispatch:

jobs:
  main-job:
    runs-on: ubuntu-latest

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          run-id: 190150094012

      - name: Process artifacts
        run: |
          ls artifacts/
          ls artifacts/synvek-explorer-artifacts-macos

